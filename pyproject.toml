[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "gova-api"
version = "1.0.0"
description = "GOVA AI Visibility Analysis API"
requires-python = ">=3.10"
